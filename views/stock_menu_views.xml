<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add Real Estate menu in Inventory module -->
    <menuitem id="menu_stock_real_estate"
              name="Real Estate"
              parent="stock.menu_stock_inventory_control"
              sequence="100"/>

    <!-- Add Apartments menu item -->
    <menuitem id="menu_stock_apartment_products"
              name="Apartments"
              parent="menu_stock_real_estate"
              action="action_real_estate_apartment_products"
              sequence="10"/>

    <!-- Add a shortcut to create apartment -->
    <record id="product_template_action_create_apartment" model="ir.actions.act_window">
        <field name="name">Create Apartment</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">product.template</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
        <field name="view_id" ref="product_template_form_view_real_estate"/>
        <field name="context">{
            'default_is_apartment': True,
            'default_type': 'product',
            'default_apartment_state': 'disponible',
            'default_rooms': 1,
            'default_bathrooms': 1,
            'default_floor': 0,
            'default_area': 0.0,
            'default_context_project_readonly': False,
            'default_context_building_readonly': False,
            'form_view_ref': 'wm_real_estate.product_template_form_view_real_estate',
            'create': True,
            'show_buttons': True
        }</field>
    </record>

    <!-- Add Buildings menu item -->
    <menuitem id="menu_stock_buildings"
              name="Buildings"
              parent="menu_stock_real_estate"
              action="action_real_estate_building"
              sequence="20"/>

    <!-- Add Projects menu item -->
    <menuitem id="menu_stock_projects"
              name="Projects"
              parent="menu_stock_real_estate"
              action="action_real_estate_project"
              sequence="30"/>
</odoo>
