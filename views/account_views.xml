<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit Form View -->
    <record id="view_move_form_real_estate" model="ir.ui.view">
        <field name="name">account.move.form.real.estate</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="project_id" attrs="{'invisible': [('move_type', '!=', 'out_invoice')]}"/>
                <field name="building_id" attrs="{'invisible': [('move_type', '!=', 'out_invoice')]}"/>
            </xpath>
        </field>
    </record>

    <!-- Inherit Tree View -->
    <record id="view_move_tree_real_estate" model="ir.ui.view">
        <field name="name">account.move.tree.real.estate</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="project_id" optional="show"/>
                <field name="building_id" optional="show"/>
            </xpath>
        </field>
    </record>

    <!-- Inherit Search View -->
    <record id="view_account_invoice_filter_real_estate" model="ir.ui.view">
        <field name="name">account.move.select.real.estate</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_invoice_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="project_id"/>
                <field name="building_id"/>
            </xpath>
            <xpath expr="//group" position="inside">
                <filter string="Project" name="group_by_project" domain="[]" context="{'group_by':'project_id'}"/>
                <filter string="Building" name="group_by_building" domain="[]" context="{'group_by':'building_id'}"/>
            </xpath>
        </field>
    </record>

    <!-- Invoice Action -->
    <record id="action_real_estate_invoices" model="ir.actions.act_window">
        <field name="name">Factures</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="view_move_tree_real_estate"/>
        <field name="search_view_id" ref="view_account_invoice_filter_real_estate"/>
        <field name="domain">[('move_type', 'in', ('out_invoice', 'out_refund')), ('state', '!=', 'cancel')]</field>
        <field name="context">{'default_move_type': 'out_invoice'}</field>        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first invoice
            </p>
        </field>
    </record>
</odoo>
